{
  "name": "futurium-suitcase-neural-numbers",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.16.0"
  },
  "dependencies": {
    "degit": "^2.8.4"
  },
  "scripts": {
    "build": "rm -rf ./neural-numbers-mini && degit github:IMAGINARY/neural-numbers-mini#v1.0.1 --mode=git --force neural-numbers-mini && cd ./neural-numbers-mini && npm install && npm run build && cd .. && rm -rf public && mv ./neural-numbers-mini/dist ./public && rm -rf ./neural-numbers-mini"
  }
}
